# Tic-Tac-Toe - Τεκμηρίωση Σχεδιασμού Κλάσεων

## Επισκόπηση Έργου
Αυτό το έγγραφο περιγράφει τη δομή κλάσεων και τις σχέσεις του παιχνιδιού τρίλιζα (*Tic-Tac-Toe*), με εφαρμογή των σχεδιαστικών προτύπων **Strategy** και **Factory**.

---

## Σχεδιαστικά Πρότυπα

### 1. Πρότυπο Strategy
Εφαρμόζεται για να επιτρέπει την εναλλαγή αλγορίθμων τεχνητής νοημοσύνης χωρίς αλλαγή στον βασικό κώδικα του παιχνιδιού.

**Συμμετέχουσες Κλάσεις:**
- `AIStrategy` (Διεπαφή)
- `RuleBasedStrategy`
- `MinimaxStrategy`
- `HeuristicStrategy`
- `AIPlayer` (Context)

### 2. Πρότυπο Factory
Εφαρμόζεται για τη δημιουργία αντικειμένων στρατηγικής. Ο κύριος κώδικας δεν δημιουργεί ποτέ απευθείας στρατηγικές — ο παραγωγός αναλαμβάνει τη δημιουργία βάσει της επιλογής του χρήστη.

**Συμμετέχουσες Κλάσεις:**
- `AIStrategyFactory`

---

## Λεπτομέρειες Κλάσεων

### Πακέτο `com.tictactoe.model`

#### Cell
- **Σκοπός:** Enum για τις πιθανές καταστάσεις ενός κελιού
- **Τιμές:** `EMPTY`, `X`, `O`
- **Πεδία:** `symbol` (char)

#### Move
- **Σκοπός:** Αναπαράσταση κίνησης στον πίνακα
- **Πεδία:** `row`, `col` (int)

#### Board
- **Σκοπός:** Διαχείριση της κατάστασης του πίνακα παιχνιδιού
- **Πεδία:** `cells` (Cell[3][3])
- **Μέθοδοι:**
  - `makeMove(...)`, `undoMove(...)`, `isValidMove(...)`
  - `isWin(Cell)`, `isDraw()`, `isGameOver()`
  - `getEmptyCells()`, `display()`, `clone()`

#### Player (Αφαιρετική)
- **Σκοπός:** Βάση για τους παίκτες (ανθρώπινος ή AI)
- **Πεδία:** `symbol` (Cell)
- **Μέθοδοι:** `getSymbol()`, `getMove(Board)` (abstract)

#### HumanPlayer
- **Κληρονομεί:** `Player`
- **Σκοπός:** Διάβασμα κινήσεων από την κονσόλα
- **Πεδία:** `scanner` (Scanner)

#### AIPlayer
- **Κληρονομεί:** `Player`
- **Σκοπός:** Ανάθεση επιλογής κίνησης στη στρατηγική
- **Πεδία:** `strategy` (AIStrategy)

### Πακέτο `com.tictactoe.strategy`

#### AIStrategy (Διεπαφή)
- `chooseMove(Board, Cell)`: Επιλέγει τη βέλτιστη κίνηση

#### RuleBasedStrategy
- **Αλγόριθμος:** Σταθερή προτεραιότητα — νίκη → μπλοκάρισμα → κέντρο → γωνίες → οποιαδήποτε θέση

#### MinimaxStrategy
- **Αλγόριθμος:** Αναδρομική αναζήτηση δέντρου παιχνιδιού. Εξετάζει όλες τις πιθανές καταστάσεις και επιλέγει την κίνηση με το βέλτιστο αποτέλεσμα.

#### HeuristicStrategy
- **Αλγόριθμος:** Βαθμολόγηση κινήσεων με βάση θεσιακή αξία (κέντρο > γωνίες > άκρες) και δυναμικότητα γραμμών (αριθμός φιλικών/εχθρικών κομματιών σε κάθε γραμμή).

### Πακέτο `com.tictactoe.factory`

#### AIStrategyFactory
- **Μέθοδος:** `createStrategy(String type)` — στατική μέθοδος που επιστρέφει `AIStrategy` βάσει του ονόματος στρατηγικής
- **Χρήση:** Η κλάση `Game` καλεί τον παραγωγό και δεν δημιουργεί ποτέ απευθείας αντικείμενα στρατηγικής

### Πακέτο `com.tictactoe.game`

#### Game
- **Σκοπός:** Διαχείριση ροής παιχνιδιού, εναλλαγής παικτών και ανίχνευσης νίκης/ισοπαλίας
- **Μέθοδοι:** `start()`, `chooseStrategy()`

#### Main
- **Σκοπός:** Σημείο εισόδου εφαρμογής
- Δημιουργεί ένα `Game` και καλεί `start()`

---

## Σχέσεις Κλάσεων

- **Κληρονομικότητα:**
  - `RuleBasedStrategy`, `MinimaxStrategy`, `HeuristicStrategy` υλοποιούν `AIStrategy`
  - `HumanPlayer`, `AIPlayer` κληρονομούν `Player`

- **Σύνθεση & Συσχέτιση:**
  - `Game` έχει `Board`, `HumanPlayer`, `AIPlayer`
  - `AIPlayer` έχει `AIStrategy`
  - `Board` έχει `Cell[3][3]` και χρησιμοποιεί `Move`

---

## Ροή Προτύπων

```
Game.start()
 │
 ├──> chooseStrategy() → χρήστης επιλέγει "2"
 │
 ├──> AIStrategyFactory.createStrategy("minimax")
 │        └──> επιστρέφει new MinimaxStrategy()     [Factory δημιουργεί στρατηγική]
 │
 ├──> new AIPlayer(Cell.O, strategy)                [Στρατηγική εγχέεται στον παίκτη]
 │
 └──> Βρόχος: human.getMove() / ai.getMove()
          └──> ai → strategy.chooseMove(board, O)   [Ανάθεση μέσω interface]
```

---

## Οφέλη Σχεδιαστικών Προτύπων

**Strategy:**
- Εναλλαξιμότητα αλγορίθμων χωρίς αλλαγή πυρήνα παιχνιδιού
- Κάθε στρατηγική είναι αυτόνομη κλάση
- Εύκολη προσθήκη νέων στρατηγικών

**Factory:**
- Αποσύνδεση δημιουργίας αντικειμένων από τη λογική παιχνιδιού
- Συγκεντρωτική διαχείριση στρατηγικών
- Επικύρωση εισόδου χρήστη

---

## Επεκτασιμότητα

Η προσθήκη νέας στρατηγικής (π.χ. Monte Carlo) απαιτεί:
1. Δημιουργία νέας κλάσης που υλοποιεί `AIStrategy`
2. Προσθήκη ενός `case` στο `AIStrategyFactory.createStrategy()`

Καμία αλλαγή σε `Board`, `Player`, `Game`, ή `Main`.

---

*Αυτό το έγγραφο μπορεί να μετατραπεί σε PDF ανοίγοντάς το σε περιβάλλον Markdown ή HTML και εκτυπώνοντάς το σε PDF.*
